import React from 'react';

import { CheckBoxIcon } from '@components/Icon';

const Feed: React.FC = () => {
  return (
    <div>
      feed
      <CheckBoxIcon />
    </div>
  );
};

export default Feed;
